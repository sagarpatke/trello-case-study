version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.9.0

    working_directory: ~/trello-case-study

    steps:
      - checkout
      - run: yarn install
      - run: yarn test
      - run: yarn build

workflows:
  version: 2
  build_and_deploy:
    jobs:
      - build